#!/usr/bin/env ruby
require 'pry'
require_relative '../lib/tic_tac_toe.rb'






def play(board)
  until over?(board) || won?(board) 
      turn(board) 
  end 
    if won?(board)
        puts "Congratulations #{winner(board)}!"
    else draw?(board)
        puts "Cats Game!"
  end
end
